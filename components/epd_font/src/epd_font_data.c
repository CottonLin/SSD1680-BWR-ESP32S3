/**
 * @file epd_font_data.c
 * @brief 字体数据文件（用户自行导入）
 * 
 * 使用说明：
 * 1. 本文件用于存放字模数据，由于字体数据过大，需要用户自行导入
 * 2. 从原 STM32 项目的 EPD_Font.h 中复制字模数据
 * 3. 按照下面的格式定义字体数据数组（二维数组格式）
 * 
 * 字模格式要求：
 * - 点阵格式：阴码
 * - 取模方式：逐行式
 * - 取模走向：顺向（高位在前）
 * - ASCII 码格式：C51
 * 
 * 字体数据结构（二维数组）：
 * - 格式：const unsigned char g_font_data[95][每字符字节数]
 * - 索引：g_font_data[0] 对应字符 0x20 (空格)
 * - 索引：g_font_data[1] 对应字符 0x21 (!)
 * - ... 依此类推
 * - 无需字体头，直接使用原 STM32 项目的数组
 */

#include "epd_font.h"

/**
 * @brief 6×8 字体数据
 * 
 * 数据大小计算：
 * - 字符数量：95 个 (0x20 ~ 0x7E)
 * - 每字符字节数：8 字节 (6×8/8)
 * - 总大小：95 × 8 = 760 字节
 * 
 * 字模数据来源：
 * 从原 STM32 项目的 asc2_0806 数组直接复制
 */
const unsigned char g_font_6x8_data[95][8] = {
    /* TODO: 用户需在此处填充 6×8 字模数据 */
    /* 从原 STM32 项目的 asc2_0806 数组复制，例如： */
    /* 字符 0x20 (空格) */
    {0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00},
    /* 字符 0x21 (!) */
    {0x00, 0x00, 0x20, 0x20, 0x00, 0x00, 0x30, 0x00},
    /* ... 继续填充所有 95 个字符 */
};

/**
 * @brief 6×12 字体数据
 * 
 * 数据大小计算：
 * - 字符数量：95 个
 * - 每字符字节数：12 字节
 * - 总大小：95 × 12 = 1140 字节
 * 
 * 字模数据来源：
 * 从原 STM32 项目的 asc2_1206 数组直接复制
 */
const unsigned char g_font_6x12_data[95][12] = {
    /* TODO: 用户需在此处填充 6×12 字模数据 */
    /* 从原 STM32 项目的 asc2_1206 数组复制 */
};

/**
 * @brief 8×16 字体数据
 * 
 * 数据大小计算：
 * - 字符数量：95 个
 * - 每字符字节数：16 字节
 * - 总大小：95 × 16 = 1520 字节
 * 
 * 字模数据来源：
 * 从原 STM32 项目的 asc2_1608 数组直接复制
 */
const unsigned char g_font_8x16_data[95][16] = {
    /* TODO: 用户需在此处填充 8×16 字模数据 */
    /* 从原 STM32 项目的 asc2_1608 数组复制 */
};

/**
 * @brief 12×24 字体数据
 * 
 * 数据大小计算：
 * - 字符数量：95 个
 * - 每字符字节数：48 字节 (12 列×24 行，每行 2 字节)
 * - 总大小：95 × 48 = 4560 字节
 * 
 * 字模数据来源：
 * 从原 STM32 项目的 asc2_2412 数组直接复制
 */
const unsigned char g_font_12x24_data[95][48] = {
    /* TODO: 用户需在此处填充 12×24 字模数据 */
    /* 从原 STM32 项目的 asc2_2412 数组复制 */
};
