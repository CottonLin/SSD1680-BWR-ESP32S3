# SSD1680 刷新机制说明

## 核心发现

**SSD1680 在写入显存数据时就已经开始刷新，而不是等待刷新命令后才开始！**

## 时间线分析

### 实际刷新流程

```
T0:        开始写入黑白显存 -> SSD1680 启动刷新引擎（屏幕开始变化）
T0+50ms:   黑白显存写入完成，刷新已进行了 50ms
T0+60ms:   开始写入红色显存 -> 刷新继续进行
T0+110ms:  红色显存写入完成，刷新已进行了 110ms
T0+120ms:  发送刷新命令（确保刷新流程正确）
T0+170ms:  刷新命令被接收
T0+30s:    刷新真正完成
```

### 用户感知的"假象"

```
用户看到：
- 写入显存时屏幕在变化（~100ms）
- 屏幕"看起来"已经好了（其实还在刷新）
- 以为 2 秒就够了（实际刷新持续到 30 秒）
```

## 为什么 0xF7 没有快速效果？

**真相：**
- 0xF4 和 0xF7 波形的**实际刷新时间相同**（都是 30 秒全刷）
- 厂家定制的 SSD1680 可能没有真正的快速刷新波形
- 所谓"快速"是因为显存写入和刷新**并行进行**

## 正确的等待时间

```c
void epd_update(void)
{
    // 发送刷新命令
    epd_write_cmd(SSD1680_DISPLAY_UPDATE_CTRL_2);
    epd_write_data(0xF4);  // 0xF4 = 全刷模式
    
    epd_write_cmd(SSD1680_MASTER_ACTIVATION);
    vTaskDelay(pdMS_TO_TICKS(50));
    
    // 必须等待 30 秒！（物理特性，无法加速）
    vTaskDelay(pdMS_TO_TICKS(30000));
}
```

## 总耗时计算

| 操作 | 耗时 | 说明 |
|------|------|------|
| 写入黑白显存 | ~50ms | 5624 字节 @ 8MHz SPI |
| 写入红色显存 | ~50ms | 5624 字节 @ 8MHz SPI |
| 发送刷新命令 | ~10ms | 2 字节命令 |
| 等待刷新完成 | 30 秒 | SSD1680 物理刷新时间 |
| **总计** | **~30.1 秒** | 无法进一步优化 |

## 优化建议

### 如果确实需要更快的刷新：

1. **局部刷新**
   - 只更新变化的区域
   - 减少显存写入时间
   - 需要 SSD1680 支持局部更新

2. **异步刷新**
   ```c
   // 发送刷新命令后做其他事
   epd_update_async();
   
   // 通过 BUSY 引脚检测完成（如果连接了）
   while (gpio_get_level(EPD_PIN_BUSY)) {
       // 做其他事情
   }
   ```

3. **联系厂家**
   - 确认是否有真正的快速刷新波形
   - 获取定制的波形配置
   - 询问是否支持局部刷新

## 重要提醒

1. **必须等待 30 秒**，否则刷新不完整会有残影
2. **进入深度睡眠前必须确保刷新完成**
3. **不要相信 0xF7 的快速刷新**（实际也是 30 秒）
4. **显存写入和刷新是并行的**（这是好事）

## 验证方法

```c
// 测试 1：记录时间戳
uint32_t start = esp_timer_get_time() / 1000;
epd_clear();
uint32_t end = esp_timer_get_time() / 1000;
printf("清屏耗时：%d ms\n", end - start);  // 应该接近 30100ms

// 测试 2：观察屏幕
// 在 epd_update() 的 30 秒延时期间，屏幕应该持续变化
// 如果提前结束延时，屏幕会有残影
```

## 总结

SSD1680 的刷新是**物理过程**，无法通过软件加速。唯一的方法是：
- 接受 30 秒的全刷时间
- 或者联系厂家获取真正的快速刷新波形配置
